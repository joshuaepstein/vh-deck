// import { Resend } from 'resend'

// declare global {
//     var resend: Resend | undefined
// }

// const resend = global.resend || new Resend(process.env.RESEND_API_KEY)

// if (process.env.NODE_ENV === 'development') global.resend = resend

// export default resend
